# Programme für xAkiitoh's Stream

Lege hier deine JavaScript und Python Programme rein!

## 📋 Anleitung

### JavaScript Programme:

- Erstelle einen Ordner für dein Programm
- Stelle sicher, dass eine `package.json` vorhanden ist
- Das Programm wird automatisch mit `npm run dev` oder `npm start` gestartet

### Python Programme:

- Erstelle einen Ordner für dein Programm
- Benenne die Hauptdatei `server.py`, `main.py`, `app.py` oder `run.py`
- Das Programm wird automatisch mit `py <dateiname>.py` gestartet

## 🎮 Beispiel-Struktur:

```
Programs/
├── TwitchBot/
│   ├── package.json
│   ├── index.js
│   └── node_modules/
├── ChatAnalyzer/
│   ├── server.py
│   ├── requirements.txt
│   └── data/
└── StreamOverlay/
    ├── package.json
    ├── src/
    └── dist/
```

Nach dem Hinzufügen von Programmen, klicke auf "Aktualisieren" im Launcher!
